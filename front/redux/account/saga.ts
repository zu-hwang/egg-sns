import { all /*, delay, fork, put, takeLatest */ } from 'redux-saga/effects';
// import axios from 'axios';
// import * as account from './actions';

export default function* accountSaga() {
  yield all([]);
  //all([ fork(사가함수),fork(사가함수 ),fork(사가함수 )])
}
